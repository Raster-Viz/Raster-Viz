create_raster.py: in order to test this python function, make sure you download the raster_tools repository in the
Git Org of Raster-Viz, and follow the directions in order to create the virtual environment rstools. Now Download the
Raster-Viz repository and in the commandline travel to where the Raster-Viz project directory is on your computer.
Make sure the rstools env is activated and type "python3 web_function/create_raster.py" in order to test the python
file.

data: This directory holds test data for raster and vector data

raster_tools: this is a package that we will use to preform raster functions. This package requires the virtual
environment rstools in order to work